`raw/` contains downloaded GDSC release files and is intentionally excluded
from version control. The directory itself is retained with `.gitkeep`.

`processed/` is reserved for derived analytical data and is also excluded from
version control.
